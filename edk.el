(require 'edk-db)
(require 'edk-ewoc)

(defvar edk-app-name "edk-app")

(defvar edk-app-buffer "*edk-app*")

;; views

(defvar edk-app-views
  '("daily view" summary type "daily")
  )


;;; window management
;; (setq truncate-partial-width-windows 50)
;; (window-total-width)

;;; common db functions
;; support multifly type of database.

;;; common ewoc functions
;;; customize views
;;; built-in time views


(provide 'edk)
