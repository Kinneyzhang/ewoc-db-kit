;;; app config
(setq edk-app-name "zksummary")

(setq edk-app-buffer "*zksummary*")

;;; db config
;; (setq edk-db-file "~/.emacs.d/ego-db/zksummary.db")

;; (setq edk-db-tables '((summary
;;                        [(id :primary-key)
;;                         (type :not-null)
;;                         (content :not-null)
;;                         (summary_time :not-null)
;;                         (create_time :not-null)])))

;;; view config

